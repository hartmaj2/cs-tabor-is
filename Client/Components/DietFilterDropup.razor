<Dropdown Color="DropdownColor.Secondary" Direction="DropdownDirection.Dropup" AutoCloseBehavior="DropdownAutoCloseBehavior.Outside">
    <DropdownToggleButton> Filter diets </DropdownToggleButton>
    <DropdownMenu Class="px-3" Style="width:150px;">
        @foreach (var dietSelection in FilterDietSelections)
        {
            <label> <input type="checkbox" @bind="dietSelection.IsSelected"> @dietSelection.Name </label> <br/>
        }
    </DropdownMenu>
</Dropdown>

@code
{
    [Parameter, EditorRequired]
    public required List<AllergenSelection> FilterDietSelections { get; set; }
}
